<%= simple_form_for(@status, html: { class: 'form-horizontal' }) do |form|
 %>
  <% if @status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@status.errors.count, "error") %> prohibited this status from being saved:</h2>

      <ul>
      <% @status.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group field">
      <span class="col-md-2"></span>
      <!--  <%= form.label :user_id,:class=>"col-md-2 control-label" %> -->
    <span class="col-md-5">
  <!--     <%= form.text_field :user_id,collection:User.all ,:class=>"form-control" %> -->
      <%= form.input :user_id,collection: User.all,label_method: :fullName %>
    </span>
  </div>
  <div class="form-input field">
    <%= form.label :comment,:class=>"control-label col-md-2" %><br>
    <span class="col-md-5">
      <%= form.text_area :comment,:class=>"form-control" %>
    </span>
  </div>
  <br/>
  <div class="actions clearfix">
    <%= form.submit "Save",class:"btn btn-default" %>
  </div>
<% end %>
